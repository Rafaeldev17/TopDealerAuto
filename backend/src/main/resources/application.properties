# Configurações de Conexão com Banco de Dados
spring.datasource.url=${jdbc:sqlserver://server-topdealer.database.windows.net:1433;database=db_topdealer;encrypt=true;trustServerCertificate=false;loginTimeout=30;}
spring.datasource.username=${Rafaeldev}
spring.datasource.password=${Raf!170508}

# Driver e Dialeto para SQL Server
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect

# Manter a tabela atualizada
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
